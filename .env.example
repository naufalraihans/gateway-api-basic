# Application Port (Railway will provide this automatically in production)
PORT=3000

# Session Storage Location (Railway Volume Path or Local Path)
# For local dev, relative path or absolute path can be used
SESSION_DIR=./storage/sessions
